# Build-time/public vars for the Web app (SvelteKit)
# These can be set in Docker build args or env_file in compose

# Base path for serving behind a subpath (e.g., through a reverse proxy). Typically empty.
BASE_PATH=

# Public API base URL used by the frontend to call the backend.
# Local default matches Backend Development bind.
# Example production:
# PUBLIC_API_BASE=https://api.example.com
PUBLIC_API_BASE=http://localhost:5240
