# Nuotti.Backend environment variables (prefix: NUOTTI_)
# Copy this file to ".env" and adjust values. Most hosts/loaders (.NET user-secrets, Docker, dotenv) will pick it up.
# These map to Nuotti.Backend.Models.NuottiOptions.

# Logical service name
NUOTTI_SERVICENAME=Nuotti.Backend

# Demo greeting to validate binding
NUOTTI_GREETING=Hello from env

# Comma-separated list of allowed CORS origins (only used in non-Development environments)
# Example: https://audience.local,https://performer.local
NUOTTI_ALLOWEDORIGINS=

# Session eviction timings (in seconds)
NUOTTI_SESSIONIDLETIMEOUTSECONDS=900
NUOTTI_SESSIONEVICTIONINTERVALSECONDS=30

# Idempotency settings
NUOTTI_IDEMPOTENCYTTLSECONDS=600
NUOTTI_IDEMPOTENCYMAXPERSESSION=128

# Common cross-service values
# Default backend public URL (used by clients). For backend itself this is informational.
NUOTTI_BACKEND=http://localhost:5240
# Default session code
NUOTTI_SESSION=dev
