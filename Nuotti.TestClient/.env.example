# Nuotti.TestClient environment variables (prefix: NUOTTI_)
# Copy to `.env` and adjust.

# Backend base URL the test client should call
NUOTTI_BACKEND=http://localhost:5240
# Default session code (optional)
NUOTTI_SESSION=dev
