# Nuotti.Performer environment variables (prefix: NUOTTI_)
# Copy to `.env` and adjust.

# Backend base URL that the Performer client should use
NUOTTI_BACKEND=http://localhost:5240
# Default session code (optional)
NUOTTI_SESSION=dev
