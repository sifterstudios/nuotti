<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="Nuotti.Projector.Views.HintView">
    
    <Grid RowDefinitions="Auto,*,Auto">
        <!-- Header -->
        <Border Grid.Row="0"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource DividerBrush}"
                BorderThickness="0,0,0,2"
                Padding="24,16">
            <StackPanel>
                <TextBlock Text="💡 Hint Time!"
                           FontSize="48"
                           FontWeight="Bold"
                           Foreground="{DynamicResource PrimaryBrush}"
                           HorizontalAlignment="Center"/>
                <StackPanel x:Name="SongInfoPanel"
                            Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            Spacing="16">
                    <TextBlock x:Name="SongTitleText"
                               Text="Song Title"
                               FontSize="24"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                    <TextBlock Text="by"
                               FontSize="20"
                               Foreground="{DynamicResource TextSecondaryBrush}"/>
                    <TextBlock x:Name="SongArtistText"
                               Text="Artist Name"
                               FontSize="24"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                </StackPanel>
            </StackPanel>
        </Border>
        
        <!-- Hints content -->
        <ScrollViewer Grid.Row="1" 
                      HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto"
                      Padding="48,32">
            <StackPanel x:Name="HintsPanel" Spacing="24">
                <!-- Hint entries will be added dynamically -->
            </StackPanel>
        </ScrollViewer>
        
        <!-- Footer -->
        <Border Grid.Row="2"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource DividerBrush}"
                BorderThickness="0,2,0,0"
                Padding="24,12">
            <TextBlock x:Name="HintCountText"
                       Text="Hint 1 of 3"
                       FontSize="20"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>
